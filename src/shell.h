// UNIX Shell - Header

// terminates user input and calls removeExtraSpace()
void validateInput(char input[]);

// returns # of arguments
int getArgumentCount(char input[]);

// removes leading, trailing, and extra inner spaces
void removeExtraSpace(char input[]);
